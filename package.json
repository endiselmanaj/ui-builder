{
  "name": "ui-builder",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "server": "tsx server/index.ts",
    "client": "npm --prefix client run dev",
    "install:all": "npm install && npm --prefix client install && npm --prefix templates/base install"
  },
  "dependencies": {
    "express": "^4.19.2",
    "get-port": "^7.1.0",
    "http-proxy-middleware": "^3.0.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.30",
    "tsx": "^4.7.1",
    "typescript": "^5.4.3"
  }
}
