# One-time: run `claude setup-token` 
# paste the long-lived OAuth token here, copy this file to `.env`, then `docker compose up`.
# Token is valid for 1 year; rotate via your account settings if leaked.
CLAUDE_CODE_OAUTH_TOKEN=
